---
import Layout from '../layouts/Layout.astro';

// Mock data for latest prompts - replace with actual data source
const latestPrompts = [
  {
    id: 1,
    title: "Instagram Story Creator",
    description: "Generate engaging Instagram stories with trending hashtags and call-to-actions",
    category: "Social Media",
    example: "Create a story about sustainable fashion with eco-friendly tips...",
    exampleImage: "/prompt-examples/instagram-story-example.svg"
  },
  {
    id: 2,
    title: "Product Description Writer",
    description: "Write compelling product descriptions that convert browsers into buyers",
    category: "Marketing",
    example: "Wireless noise-cancelling headphones that immerse you in pure sound...",
    exampleImage: "/prompt-examples/product-description-example.svg"
  },
  {
    id: 3,
    title: "Character Art Generator",
    description: "Design unique character concepts with detailed visual descriptions",
    category: "Art",
    example: "A cyberpunk detective with neon blue hair and augmented reality glasses...",
    exampleImage: "/prompt-examples/character-art-example.svg"
  }
];

const talents = [
  {
    icon: "🎨",
    title: "Graphic Design",
    description: "Flyers, logos, branding materials, and creative visuals that capture attention and drive results"
  },
  {
    icon: "💻",
    title: "Web Development", 
    description: "Lightning-fast websites and web apps built with modern technologies"
  },
  {
    icon: "✨",
    title: "Content Creation",
    description: "AI-powered content strategies, prompts, and digital storytelling"
  }
];
---

<Layout>
  <!-- Hero Section -->
  <section class="hero-bg min-h-screen flex items-center justify-center relative overflow-hidden">
    <!-- Matrix-style animated background -->
    <div class="absolute inset-0 opacity-20">
      <!-- Digital rain effect -->
      <div class="matrix-rain absolute inset-0"></div>
      
      <!-- Animated grid lines -->
      <div class="absolute inset-0" style="background-image: linear-gradient(rgba(0, 255, 100, 0.1) 1px, transparent 1px), linear-gradient(90deg, rgba(0, 255, 100, 0.1) 1px, transparent 1px); background-size: 50px 50px; animation: matrix-grid 20s linear infinite;"></div>
      
      <!-- Floating particles -->
      <div class="absolute top-1/4 left-1/6 w-2 h-2 bg-green-400 rounded-full animate-pulse opacity-60"></div>
      <div class="absolute top-3/4 right-1/4 w-1 h-1 bg-cyan-400 rounded-full animate-ping opacity-40" style="animation-delay: 1s;"></div>
      <div class="absolute bottom-1/3 left-1/3 w-3 h-3 bg-orange-500 rounded-full animate-bounce opacity-50" style="animation-delay: 2s;"></div>
      <div class="absolute top-1/2 right-1/6 w-1 h-1 bg-green-300 rounded-full animate-pulse opacity-70" style="animation-delay: 3s;"></div>
      
      <!-- Glitch effect lines -->
      <div class="absolute top-0 left-0 w-full h-px bg-gradient-to-r from-transparent via-green-400 to-transparent animate-pulse opacity-30"></div>
      <div class="absolute bottom-0 left-0 w-full h-px bg-gradient-to-r from-transparent via-orange-500 to-transparent animate-pulse opacity-30" style="animation-delay: 2s;"></div>
    </div>
    
    <div class="container-custom text-center relative z-10">
      <div class="max-w-4xl mx-auto space-y-8">
        <!-- Main Headline -->
        <h1 class="heading-1">
          Welcome to <span class="text-gradient">TESTING 2 AskMajinn</span>
        </h1>
        <h2 class="text-xl md:text-2xl lg:text-3xl text-slate-300 font-light">
          Your TEST Command Center
        </h2>
        
        <!-- Subtext -->
        <p class="text-lg md:text-xl text-slate-400 max-w-2xl mx-auto leading-relaxed">
          Hub for creative talents, AI prompts, and digital innovation. 
          Explore design projects, powerful prompts, and cutting-edge content.
        </p>
        
        <!-- CTAs -->
        <div class="flex flex-col sm:flex-row gap-4 justify-center pt-8">
          <a href="/portfolio" class="btn btn-primary text-lg px-8 py-4">
            Explore My Work
          </a>
          <a href="/prompts" class="btn btn-secondary text-lg px-8 py-4">
            Visit Prompt Forum
          </a>
        </div>
      </div>
    </div>
    
    <!-- Scroll indicator -->
    <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2 animate-bounce">
      <svg class="w-6 h-6 text-slate-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3"></path>
      </svg>
    </div>
  </section>
  
  <!-- Talents Section -->
  <section class="section-padding bg-slate-900/50">
    <div class="container-custom">
      <div class="text-center mb-16">
        <h2 class="heading-2 mb-6">Core Talents</h2>
        <p class="text-xl text-slate-400 max-w-2xl mx-auto">
          Combining creativity with technology to deliver exceptional digital experiences
        </p>
      </div>
      
      <div class="grid md:grid-cols-3 gap-8">
        {talents.map((talent) => (
          <div class="card card-hover-cyan text-center group">
            <div class="text-5xl mb-6 group-hover:scale-110 transition-transform duration-200">
              {talent.icon}
            </div>
            <h3 class="heading-3 mb-4">{talent.title}</h3>
            <p class="text-slate-400 leading-relaxed">{talent.description}</p>
          </div>
        ))}
      </div>
    </div>
  </section>
  
  <!-- Prompt Forum Preview -->
  <section class="section-padding">
    <div class="container-custom">
      <div class="flex flex-col md:flex-row justify-between items-center mb-12">
        <div>
          <h2 class="heading-2 mb-4">Latest Prompts</h2>
          <p class="text-xl text-slate-400">
            Discover powerful AI prompts for creativity and productivity
          </p>
        </div>
        <a href="/prompts" class="btn btn-magenta mt-4 md:mt-0">
          See All Prompts
        </a>
      </div>
      
      <div class="grid md:grid-cols-3 gap-8">
        {latestPrompts.map((prompt) => (
          <div class="card card-hover-magenta group">
            <div class="flex items-center justify-between mb-4">
              <span class="text-sm px-3 py-1 rounded-full bg-pink-500/20 text-pink-400 border border-pink-500/30">
                {prompt.category}
              </span>
              <svg class="w-5 h-5 text-slate-400 group-hover:text-pink-400 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
              </svg>
            </div>
            <h3 class="text-xl font-semibold mb-3 group-hover:text-pink-400 transition-colors">
              {prompt.title}
            </h3>
            <p class="text-slate-400 mb-4 leading-relaxed">
              {prompt.description}
            </p>
            <div class="p-3 bg-slate-800/50 rounded-lg border border-slate-700 mb-4">
              <p class="text-sm text-slate-300 italic">
                "{prompt.example}"
              </p>
            </div>
            <div class="flex justify-center">
              <button 
                class="btn btn-secondary btn-sm view-example-btn"
                data-example-image={prompt.exampleImage}
                data-example-title={prompt.title}
              >
                👁️ View Example
              </button>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>
  
  <!-- Social/Media Grid -->
  <section class="section-padding bg-slate-900/50">
    <div class="container-custom">
      <div class="text-center mb-16">
        <h2 class="heading-2 mb-6">Connect Everywhere</h2>
        <p class="text-xl text-slate-400 max-w-2xl mx-auto">
          Follow the journey across all platforms and discover new content daily
        </p>
      </div>
      
      <div class="grid grid-cols-2 md:grid-cols-4 gap-6">
        <a href="/connect" class="card card-hover-cyan group text-center">
          <div class="text-3xl mb-3 group-hover:scale-110 transition-transform duration-200">📱</div>
          <h4 class="font-semibold">Instagram</h4>
          <p class="text-sm text-slate-400 mt-1">Daily creativity</p>
        </a>
        
        <a href="/connect" class="card card-hover-cyan group text-center">
          <div class="text-3xl mb-3 group-hover:scale-110 transition-transform duration-200">🐦</div>
          <h4 class="font-semibold">Twitter</h4>
          <p class="text-sm text-slate-400 mt-1">Quick insights</p>
        </a>
        
        <a href="/connect" class="card card-hover-cyan group text-center">
          <div class="text-3xl mb-3 group-hover:scale-110 transition-transform duration-200">🎥</div>
          <h4 class="font-semibold">YouTube</h4>
          <p class="text-sm text-slate-400 mt-1">Video tutorials</p>
        </a>
        
        <a href="/connect" class="card card-hover-cyan group text-center">
          <div class="text-3xl mb-3 group-hover:scale-110 transition-transform duration-200">💼</div>
          <h4 class="font-semibold">LinkedIn</h4>
          <p class="text-sm text-slate-400 mt-1">Professional</p>
        </a>
      </div>
    </div>
  </section>
  
  <!-- CTA Section -->
  <section class="section-padding">
    <div class="container-custom">
      <div class="card card-hover-cyan text-center max-w-3xl mx-auto bg-gradient-to-r from-cyan-500/10 to-pink-500/10 border-cyan-400/30">
        <h2 class="heading-2 mb-6">Ready to Create Something Amazing?</h2>
        <p class="text-xl text-slate-300 mb-8 max-w-2xl mx-auto">
          Whether you need design work, want to explore AI prompts, or learn new skills, 
          let's connect and bring your ideas to life.
        </p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <a href="/contact" class="btn btn-primary text-lg px-8 py-4">
            Get In Touch
          </a>
          <a href="/learn" class="btn btn-secondary text-lg px-8 py-4">
            Start Learning
          </a>
        </div>
      </div>
    </div>
  </section>
</Layout>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    // View example functionality for homepage
    const exampleButtons = document.querySelectorAll('.view-example-btn');
    
    exampleButtons.forEach(btn => {
      btn.addEventListener('click', () => {
        const exampleImage = btn.getAttribute('data-example-image');
        const exampleTitle = btn.getAttribute('data-example-title');
        
        // Create modal
        const modal = document.createElement('div');
        modal.className = 'fixed inset-0 bg-black/80 flex items-center justify-center z-50 p-4';
        modal.innerHTML = `
          <div class="bg-slate-900 rounded-lg border border-orange-500/30 max-w-4xl w-full max-h-[90vh] overflow-auto">
            <div class="p-6">
              <div class="flex justify-between items-center mb-4">
                <h3 class="text-xl font-semibold text-gradient">${exampleTitle} - Example</h3>
                <button class="close-modal text-2xl hover:text-orange-400 transition-colors">&times;</button>
              </div>
              <img src="${exampleImage}" alt="${exampleTitle} Example" class="w-full rounded-lg border border-slate-700" />
            </div>
          </div>
        `;
        
        document.body.appendChild(modal);
        
        // Close modal functionality
        const closeModal = () => {
          document.body.removeChild(modal);
        };
        
        modal.querySelector('.close-modal').addEventListener('click', closeModal);
        modal.addEventListener('click', (e) => {
          if (e.target === modal) closeModal();
        });
        
        // Close on Escape key
        const handleEscape = (e) => {
          if (e.key === 'Escape') {
            closeModal();
            document.removeEventListener('keydown', handleEscape);
          }
        };
        document.addEventListener('keydown', handleEscape);
      });
    });
  });
</script>